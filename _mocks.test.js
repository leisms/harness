// This pre-test setup routine is recommended to help
// manage mocks in one place prior to testing.

global.mocks = {
    // Your mocks go here as key-value pairs.
    // e.g. foo: { doThis: function(){}, doThat: function(){} }
}